---
import { getCollection } from 'astro:content';

const recepten = await getCollection('recepten');
---

<table>
<tr>
<th>Recept</th>
<th>Gang</th>
</tr>

{recepten.map((recept) => <tr><td><a href={recept.slug}>{recept.data.title}</a> </td> <td>{recept.data.recipeCategory}</td></tr>)}

</table>